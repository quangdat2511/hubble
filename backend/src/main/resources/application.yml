server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres?sslmode=require
    username: postgres.qexzpkwrfjlblrvtqvuh
    password: hubble@56183
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6379

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 100MB

jwt:
  secret: hubble-super-secret-key-change-this-in-production
  access-token-expiration: 3600000
  refresh-token-expiration: 2592000000

aws:
  s3:
    bucket-name: hubble-media
    region: ap-southeast-1
    access-key: YOUR_AWS_ACCESS_KEY
    secret-key: YOUR_AWS_SECRET_KEY

logging:
  level:
    com.hubble: DEBUG
